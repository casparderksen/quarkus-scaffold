/**
 * Database access implementations.
 *
 * <p>Split into {@code jpa} (aggregate repositories using Hibernate) and {@code query} (read-side
 * optimized queries and projections).
 */
package org.example.boundedcontext.infrastructure.adapter.out.persistence;
