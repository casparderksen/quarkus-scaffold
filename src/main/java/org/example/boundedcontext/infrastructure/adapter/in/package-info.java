/**
 * Inbound (driving) adapters: REST controllers, Kafka consumers, schedulers.
 *
 * <p>Depend only on {@code application.port.in}. Entrypoints only — no domain logic.
 */
package org.example.boundedcontext.infrastructure.adapter.in;
