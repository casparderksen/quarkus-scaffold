/**
 * Domain core: aggregates, entities, value objects, domain services, and events.
 *
 * <p>Independent from frameworks and infrastructure, except for JPA annotations (pragmatic exception
 * to preserve Hibernate optimizations). Must not depend on {@code application} or
 * {@code infrastructure}.
 */
package org.example.boundedcontext.domain;
