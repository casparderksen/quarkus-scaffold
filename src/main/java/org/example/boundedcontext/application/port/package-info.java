/**
 * Hexagonal ports: abstractions defining communication with the application core.
 *
 * <p>Inbound ports ({@code in}) expose application capabilities; outbound ports ({@code out})
 * describe required external dependencies.
 */
package org.example.boundedcontext.application.port;
