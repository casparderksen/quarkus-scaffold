/**
 * Inbound ports: contracts exposing application capabilities to external actors.
 *
 * <p>Represent use case interfaces. Split into {@code command} (state-changing) and {@code query}
 * (read-only).
 */
package org.example.boundedcontext.application.port.in;
