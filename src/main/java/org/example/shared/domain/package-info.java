/**
 * Shared domain layer: the shared kernel ({@code kernel}) and shared abstract business exceptions
 * ({@code exception}).
 *
 * <p>Scope is restricted to stable primitives and base abstractions reused across bounded contexts.
 * Bounded-context-specific domain logic does not belong here.
 */
package org.example.shared.domain;
