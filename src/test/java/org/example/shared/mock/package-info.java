/**
 * Shared mocks, stubs, and fakes used across bounded contexts.
 *
 * <p>Fake: lightweight working implementation (e.g., in-memory repositories). Stub: predefined
 * responses, no verification. Mock: verifies interactions.
 *
 * <p>Naming convention: {@code Fake<Port>}, {@code Stub<Port>}, {@code Mock<Port>}.
 */
package org.example.shared.mock;
